[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
norecursedirs = src
addopts =
    --verbose
    --tb=short
    --cov=src/muteme_btn
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-fail-under=85
asyncio_mode = auto
